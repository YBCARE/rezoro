services:
  - type: web
    name: rezoro
    runtime: node
    buildCommand: cd server && npm install
    startCommand: node server/server.js
    envVars:
      - key: NODE_OPTIONS
        value: --dns-result-order=ipv6first
